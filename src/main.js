import { App } from './App.js';

/**
 * @fileoverview Main entry point for the application.
 * Creates a new instance of the App class when the window loads.
 */

window.onload = () => {
    new App();
};
